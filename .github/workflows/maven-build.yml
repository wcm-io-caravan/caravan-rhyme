# Build validation

name: Build

on:
  push:
    branches-ignore:
      - master
      - experimental/**
  pull_request:
    branches-ignore:
      - master
      - experimental/**
  workflow_dispatch:

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        java: [8, 11]
        os: [ubuntu-latest]
        distribution: [temurin]

    steps:
      # Check out Git repository
      - name: Checkout code
        uses: actions/checkout@v2

      # Set up environment with Java and Maven
      - name: Setup JDK
        uses: actions/setup-java@v2
        with:
          distribution: ${{ matrix.distribution }}
          java-version: ${{ matrix.java }}
          cache: 'maven'

      # Build & install (install target is used rather than verify to ensure that modules from the main build are available for the examples builds)
      - name: Build and install
        run: ./mvnw -s ./.maven-settings.xml -Pcontinuous-integration -B -U clean install

      # Run code coverage check
      - name: Run code coverage check
        run: bash <(curl -s https://codecov.io/bash)
